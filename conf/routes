# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                                       Application.Index
GET		/posts/:key								Application.Posts
GET     /images/:key/*path						Application.Static
GET		/rss									Application.RSS
POST	/compile								Application.Play
POST	/share									Application.Play

# Ignore favicon requests
GET     /favicon.ico							Static.Serve("public/images","favicon.jpeg")

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath						Static.Serve("public")

# Catch all
*       /:controller/:action					:controller.:action
